<%= render :template => 'dashboard/index' -%>
<p style="color: green"><%= notice %></p>
 <li class="nav-item active">
          <a>  <%= link_to " Go to see your friends or delete ot", your_friends_path %> </a>
        </li>
        <li class="nav-item active">
         <a>  <%= link_to " Send petitions", possibles_friends_path %> </a>
        </li>
       <li class="nav-item active">
         <a>  <%= link_to " Accept petitions", send_request_path %> </a>
        </li>

   <% if current_user.admin?%> 
      <h2>welcome admin, choose friends to delete</h2>
      <ul>
      <% @users.each do |user| %>
      <% if user.friend_id? %>
        <% for friendships in @user.friendships %>
          <li>
          <%= h friendship.friend.Username %>
          (<%= link_to "remove", friendship, :method => :delete %>)
          </li>
        <% end %>  
      <% end %>
      </ul>
    <% end%>
  <% end%>
<%= link_to "Back to users", users_path %>
</div>

